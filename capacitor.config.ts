import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pim_lek8_2022_v1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
